id: no-as-any
language: TypeScript
severity: error
message: "Type-system escape hatch: use a type guard or narrow the type instead of `as any`."
rule:
  any:
    - pattern: $E as any
    - pattern: $E as unknown as $T
